import { StorageAdapter } from './storage-adapter';
export declare class LocalStorageAdapter implements StorageAdapter {
    storedData: any[];
    storageName: string;
    localStorage: any;
    /**
     * init local storage
     *
     * @returns       any
     */
    init(storageName: string): Promise<void>;
    /**
     * set storage value
     *
     * @param cache   Comment describing the `value` parameter.
     * @returns void
     */
    set(cache: any[]): Promise<void>;
    /**
     * push element to local storage memory
     *
     * @param el    the pushed element `value`.
     * @returns void
     */
    push(el: any): Promise<void>;
    /**
     * shift element from the local storage memory
     *
     * @returns A promise that is resolved with the element value.
     */
    shift(): Promise<any>;
    /**
     * get storage value
     *
     * @returns   MemoryMap Object
     */
    get(): Promise<any[]>;
    /**
     * delete object from local storage
     *
     * @returns       void
     */
    reset(): Promise<void>;
    /**
     * persist local storage data
     *
     * @returns void
     */
    private __persist;
}
