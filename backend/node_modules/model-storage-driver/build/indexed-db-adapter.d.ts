import { StorageAdapter } from './storage-adapter';
export declare class IndexedDbAdapter implements StorageAdapter {
    storedData: any[];
    storageName: string;
    private memoryCache;
    /**
     * init indexedDb storage
     *
     * @returns       any
     */
    init(storageName: string): Promise<void>;
    /**
     * set storage value
     *
     * @param cache new value for storedData
     * @returns void
     */
    set(cache: any[]): Promise<void>;
    /**
     * push element to indexedDb storage memory
     *
     * @param el    the pushed element `value`.
     * @returns void
     */
    push(el: any): Promise<void>;
    /**
     * shift element from the indexedDb storage
     *
     * @returns A promise that is resolved with the element value.
     */
    shift(): Promise<any>;
    /**
     * get storage value
     *
     * @returns   MemoryMap Object
     */
    get(): Promise<any[]>;
    /**
     * delete all objects from indexedDb storage
     *
     * @returns       void
     */
    reset(): Promise<void>;
    /**
     * persist indexedDb storage data
     *
     * @returns void
     */
    private __persist;
}
